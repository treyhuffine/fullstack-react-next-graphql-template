export enum Role {
  Admin = 'admin',
  User = 'user',
  Editor = 'editor',
  Unknown = 'unknown',
  Public = 'public',
}

export enum Theme {
  Dark = 'dark',
  Light = 'light',
}

export enum AuthSource {
  Google = 'google',
  Facebook = 'facebook',
  Twitter = 'twitter',
  Instagram = 'instagram',
  Unknown = 'unknown',
}
